#pragma once

// ===== Effect entry points =====
//
// One run<Name>() function per effect, defined in its own .cpp
// under src/effects/. Every new effect must be declared here.

void runBouncingBall();
